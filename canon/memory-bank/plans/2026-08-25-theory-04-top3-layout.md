# План: Глава 04 — панель на всю ширину, центр

**Дата:** 2026-08-25  
**Статус:** готово  
**Глава:** 04 Топ-3 (`id: top3`)  
**URL:** `http://localhost:5173/?tab=theory&chapter=top3`

## Цель

Открытая **глава 04** в `#chapter-panel`: лекция не узкая левая полоса `42em`. Текст на ширину `.ta-chapter-panel__body` с отступом от края. Заголовки и схемы (chips формулы, подиум) по центру. Справа нет пустоты из‑за искусственного лимита. На 390px нет overflow-x.

## Критерий готовности

- `?tab=theory&chapter=top3` desktop: колонка текста = ширина body минус padding.
- Схема `.ta-pattern-formula` по центру, не слева.
- Заголовки intro/section/topic по центру.
- Виджет `Top3Podium` не ломает ширину и не даёт overflow-x на 390px.
- 390px: нет горизонтального скролла панели.
- Общий блок `.ta-chapter-panel__body[data-chapter]` не переписывать. Копию лекции не трогать. Другие главы не трогать. Коммит не делать.

## Пункты

1. Записать этот план (цель + критерий).
2. Скрыть Eruda, замерить ширины body/lede/формул/подиума, overflow; скрин desktop + 390px.
3. Если дыра только у виджета подиума — точечный CSS под `[data-chapter='top3']`. Иначе стоп и отчёт.
4. Повторный замер + скрин. Вернуть ширины, overflow, файлы, leftovers.

## Запрещено в этом круге

Главы 01–03 и 05–10, rewrite copy, rewrite shared `[data-chapter]` block, git commit.
