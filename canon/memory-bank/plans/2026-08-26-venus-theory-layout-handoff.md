# План: отдать Venus layout Теории + инструкцию

**Дата:** 2026-08-26  
**Статус:** готово — SSH copy OK; SMB не смонтирован

## Цель

Довезти до Venus (Windows) уже готовый layout панелей Теории (главы 02–10 + 3 reading) и положить датированную инструкцию в `D:\Mac\OUTBOX\`, чтобы Venus Cursor видел, что село, и не переделывал CSS/copy. Коммит не делать. Тексты лекций не трогать.

## Критерий готовности

- Инструкция на русском лежит в OUTBOX (и локальная копия в `audit/`).
- На Venus в `C:\Users\user\Desktop\React_Project` обновлены `theory-apple.css` и `TheoryChapterPanel.tsx`.
- Если SMB `MacVenus_Bridge` недоступен — доставка по SSH; путь зафиксирован.
- Коммита нет.

## Пункты

1. Inbox pull (скрипт отсутствует — одна строка в лог, дальше). ✓
2. Проверить SSH `venus` и `D:\Mac\OUTBOX`. ✓
3. Написать инструкцию: селекторы, запреты, не Clomiphene. ✓
4. scp/rsync файлов + PDF. ✓ SSH; SMB не смонтирован.
5. Сообщить пути: удалось / локальный fallback.

**Цель достигнута.** Качество: файлы на Venus совпали по размеру с Mac (css 128856, tsx 3798). Коммита нет.
