# План: полный дамп на Venus + лог + статус дисков

**Дата:** 2026-08-26  
**Статус:** готово — overlay + логи + v0.003 на дисках

## Цель

На Venus лежит актуальная рабочая копия приложения с Mac (все обновы после v0.002 / снимка 08-08 / partial layout 08-26), в `D:\Mac\OUTBOX` — датированная инструкция и лог, на дисках — честный статус занятости. Коммит не делать. Секреты и `gym.db` на Venus не затирать.

## Критерий готовности

- `C:\Users\user\Desktop\React_Project` обновлён overlay-тарболом (без `.env*`, `gym.db`, `node_modules`, `.git`).
- `D:\Mac\REACT_LATEST\source.tar.gz` + README обновлены.
- `D:\Mac\OUTBOX\2026-08-26-*-mac-full-sync.md` (+ PDF) и запись в `VENUS-AGENT-LOG.md`.
- Реестр версий: v0.003 на дисках (если место/путь живы) и в OUTBOX.
- В ответе человеку: факт по дискам (1 ТБ vs 500) и что уехало на Venus.

## Пункты

1. Inbox pull — скрипта нет, skip.
2. tar с исключениями → scp → extract на Desktop\React_Project.
3. Скопировать tar в REACT_LATEST + versions v0.003.
4. Логи OUTBOX + PDF; обновить статус-файлы на дисках.
5. Сообщить пути и занятость томов. ✓

**Цель достигнута.** Overlay на Desktop, tar в REACT_LATEST (SHA256 совпал), логи в OUTBOX + PDF, v0.003 на обоих дисках, статус дисков обновлён. Коммита нет. `.env*` / `gym.db` не затирались.
