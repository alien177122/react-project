# План: платежи — уже есть контур, провайдера не менять

**Дата:** 2026-09-08
**Цель:** не строить вторую оплату. Сначала понять текущий entitlement, UI и визуал не трогать.

## Уже в коде

- `users.premium` — флаг доступа.
- `billing_payments` — PENDING / CONFIRMED / CANCELED, валюта RUB.
- Провайдер: Platega (`server/billing.js`), включается env. Без env checkout отвечает 503.
- `GET /api/billing/status` отдаёт premium и remaining.
- Калькулятор уже читает `billingStatus` (`CalculatorTab.tsx`).
- Лимит бесплатных расчётов считается в `server/db.js`.

## Не делать

- Не подключать ЮKassa / Kaspi / Stripe поверх Platega.
- Не рисовать новый экран оплаты, пока не скажут, что включать.
- Не двигать `docs` / `prompts` / `subagents`.

## Следующий узкий шаг, когда скажут

1. Проверить, включён ли Platega в env и какой лимит бесплатных расчётов.
2. Только после этого — кнопка оплаты в калькуляторе, если её ещё нет в текущем экране.
