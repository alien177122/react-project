# Backlog

Осознанно отложенные улучшения. Не баги, не критичные фиксы — точки роста для будущих спринтов.

> 🔄 Этот файл живёт только если:
> 1. Каждый пункт имеет владельца (инициалы) и оценку усилий (S/M/L) — добавляются при триаже.
> 2. Пункты старше 90 дней без движения архивируются в `backlog.archive.md`.
> 3. Перед добавлением нового — удаляем один устаревший или переносим в архив.

---

## Глава 09 — Прогрессия 2.0

### Производительность
- [ ] `useReveal`: отписка через `unobserve()` после первого срабатывания
- [ ] `will-change: transform, opacity` ставить on-demand через JS, снимать после `.is-revealed`
- [ ] Stagger для `RevealTimeline` только при `!prefers-reduced-motion`

### Визуал / CSS
- [ ] `color-mix(in oklch, ...)` для `--ta-sec-09-tint` + `@supports` fallback на `rgba()`
- [ ] Вынести ширину ярусов `TierPyramid` в `data-tier-width` + `attr()` в CSS (сейчас хардкод классов `.ta-tier--I…V`)
- [ ] Проверить контраст индиго `#6366f1` на `#0a0c10` через `oklch`, при необходимости сдвинуть lightness

### Доступность
- [ ] `role="list"` на контейнер `RevealTimeline`, `role="listitem"` на узлы — только если узлы станут интерактивными
- [ ] `aria-controls` не использовать для скролла — семантически неверно; оставить `aria-label` + `scrollTo()`

### Расширения
- [ ] URL-state: `?tier=meso&model=wave` для глубоких ссылок на слайды теории
- [ ] `@media print` стили для `TierPyramid`: вертикальный стек, без анимаций, чёрно-белая палитра
- [ ] i18n-ready: вынести строки data-слоя как `id: string`, маппинг `en/ru` на уровень приложения

### Качество / Автоматизация
- [ ] Подключить `axe-core` в dev-режиме для Theory tab
- [ ] Добавить Lighthouse CI-чек: LCP <2.5s, CLS <0.1, контраст ≥ AA
- [ ] Автотест на уникальность `id` в `SPECIAL_METHODS` (`tests/periodization-data.test.ts`)
- [ ] Скрипт `npm run backlog:prune` — находит пункты без `[ ]` или старше 90 дней по дате коммита, печатает напоминание

---

> Принцип: не добавляем сложность в текущий спринт. Каждый пункт — отдельная задача с оценкой влияния на метрики.
