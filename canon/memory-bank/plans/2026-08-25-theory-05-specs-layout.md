# План: Глава 05 — панель на всю ширину, центр

**Дата:** 2026-08-25  
**Статус:** готово

## Цель

Открытая **глава 05** (`specs`, «%ПМ + RPE») в `#chapter-panel`: лекция на ширину `.ta-chapter-panel__body` (общий блок `[data-chapter]` уже есть — не трогать). Уникальные виджеты главы — таблицы Intensity (% от 1ПМ) и Effort (шкала RPE) в `SpecsTables` — на всю ширину панели, заголовки карточек и схемы по центру. На 390px нет overflow-x. Текст лекции не переписывать.

## Критерий готовности

- `?tab=theory&chapter=specs` desktop: `.ta-specs` ширина = body минус padding; две карточки не уже лекции.
- Заголовки `.ta-specs-card-head` / eyebrow / title по центру.
- Полосы `%ПМ` и строки RPE не вылезают за карточку.
- 390px: `overflow-x` панели = 0.
- Общий блок `.ta-chapter-panel__body[data-chapter]` не переписан. Copy лекции не тронут. Коммит не делается.

## Пункты

1. Скрыть Eruda, замерить desktop + 390px, скриншоты.
2. CSS только под `[data-chapter='specs']`: `.ta-specs`, карточки, `.ta-bar-*`, `.ta-rpe-*`.
3. Повторный замер и скрин. Если дыра вне виджета — стоп и отчёт, без правки общего блока.

## Результат

| Viewport | body | intro | `.ta-specs`  | overflow-x |
| -------- | ---- | ----- | ------------ | ---------- |
| 1280     | 1136 | 1072  | 1072 (2×522) | 0          |
| 390      | 350  | 310   | 310 (1 col)  | 0          |

Заголовки карточек: `text-align: center`. Copy лекции не менялся. Коммит не делался.
