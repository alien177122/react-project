# План: система статей Теории (дизайн + алгоритм + лексикон)

**Дата:** 2026-08-26  
**Статус:** готово

## Цель

Собрать воспроизводимую систему написания статей вкладки «Теория»: визуальный рецепт с живого UI, алгоритм сборки каждой статьи и общий лексикон. Источник редакции — файл аудита фитнес-контента; канон продукта — structurer + lecture page + frac. Главы 01, 02 и блок `.ta-frac` **не перестраивать**.

## Критерий готовности

1. Есть `memory-bank/reference/theory-article-algorithm.md` (дизайн, алгоритм, лексикон, agent prompt).
2. Рядом PDF с полями ≤ 0.5 см.
3. Индекс `memory-bank/reference/README.md` и skill `theory-content-structurer` ссылаются на алгоритм.
4. `memory_save` зафиксировал lock: 01 stack + 02 body + frac.
5. CSS/markup 01, 02, `LectureSection`, `.ta-frac` не менялись в этом проходе.

## Порядок

1. Сверка Downloads-алгоритма с каноном Теории (принять / отвергнуть).
2. Зафиксировать visual recipe с locked UI.
3. Свернуть editorial pipeline в нумерованную процедуру агента.
4. Собрать лексикон из `theory.ts`.
5. MD + PDF + индекс + memory_save.
6. Не рестайлить 03–10 в этом проходе.
