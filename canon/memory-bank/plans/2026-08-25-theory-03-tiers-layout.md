# План: Глава 03 — панель на всю ширину, центр

**Дата:** 2026-08-25  
**Статус:** готово — цель сверена (desktop S=1072px = тело лекции; 390 overflow-x=0)

## Цель

Открытая **глава 03** (`id: tiers`) в `#chapter-panel`: текст лекции на ширину `.ta-chapter-panel__body` с отступом от края, не узкая левая полоса. Заголовки и схема пирамиды S–F по центру. Справа нет пустоты из‑за искусственного лимита. На 390px нет overflow-x.

## Критерий готовности

- `?tab=theory&chapter=tiers` desktop: колонка текста = ширина body минус padding.
- Заголовки intro/section/topic по центру (общий `[data-chapter]` CSS координатора).
- Уникальный виджет: `.ta-pyramid` / `.ta-tier` на всю ширину панели, ступени центрированы; `RevealTimeline` в этой главе нет.
- 390px: нет горизонтального скролла панели.
- Общий блок `.ta-chapter-panel__body[data-chapter]` не переписывать. `theory.ts` не трогать. Другие главы не править.

## Пункты

1. Записать этот план.
2. Скрыть Eruda, замерить desktop + 390px, скриншоты.
3. Если дыра только у пирамиды (`max-width: 920px` на `.ta-tier`) — точечный CSS `[data-chapter='tiers']`. Иначе стоп и отчёт.
4. Вернуть ширины, overflow, файлы, leftovers.

## Вне скоупа

Главы 01–02 и 04–10, copy в `theory.ts`, коммит, общий `[data-chapter]` блок.
